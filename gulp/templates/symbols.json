{
  "symbols": [<% _.forEach(icons, function(icon, index){ if(index > 0){ %>,<% }; %>
      {
        "id": "<%= icon.id %>",
        "width": "<%= icon.width %>",
        "height": "<%= icon.height %>"
      }<% }); %>
  ]
}
