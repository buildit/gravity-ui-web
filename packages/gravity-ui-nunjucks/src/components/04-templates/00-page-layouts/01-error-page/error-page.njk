{%- extends '@basic-page' -%}

{%- from '@cta' import cta -%}
{%- from '@ascii-art' import asciiArt -%}

{%- block pageContent %}
  <main class="grav-o-full-bleed__content grav-u-pb-xl grav-u-pt-l grav-u-text-centered">
    {{ asciiArt( ascii ) | safe }}
    <h1>{{title}}</h1>
    <p>{{text}}</p>
    <div>
      {{ cta(homeLink.text, homeLink.url) | safe }}
    </div>
  </main>
{%- endblock -%}
