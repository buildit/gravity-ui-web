{
  "symbols": [<% _.forEach(icons, function(icon, index){ if(index > 0){ %>,<% }; %>
      {
        "symbolId": "<%= icon.id %>",
        "titleId": "<%= icon.id %><%= icon.titleIdSuffix %>",
        "width": "<%= icon.width %>",
        "height": "<%= icon.height %>"
      }<% }); %>
  ]
}
