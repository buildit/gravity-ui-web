<!DOCTYPE html>
<html lang="en"{{#htmlClass}} class="{{ htmlClass }}"{{/htmlClass}}>
  <head>
    <title>{{ title }} - Buildit</title>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width" />

    <link rel="stylesheet" href="../../ui-lib/gravity.css?{{ cacheBuster }}" media="all" />
    <link rel="stylesheet" href="../../ui-lib/debug.css?{{ cacheBuster }}" media="all" />


    <!-- Begin Pattern Lab (Required for Pattern Lab to run properly) -->
    <link rel="stylesheet" href="../../css/pattern-scaffolding.css?{{ cacheBuster }}" media="all" />
    {{{ patternLabHead }}}
    <!-- End Pattern Lab -->

  </head>
  <body{{#bodyClass}} class="{{ bodyClass }}"{{/bodyClass}}>

