{%- from 'atoms-image' import image -%}

{# Reusable macro for other templates to import: #}
{%- macro logoImage(src, alt) -%}
  {{ image(src, alt, "grav-c-logo-image") | safe }}
{%- endmacro -%}

{# Example rendering: #}
{{- logoImage(img.logo.src, img.logo.alt) | safe -}}
