{%- from 'atoms-button' import button -%}
{%- from 'molecules-form-group' import formGroup -%}
<form>
  <h2>{{form.title}}</h2>
  {% if form.title -%}
  {%- endif %}
  {% for group in form.groups -%}
      {{ formGroup(group) }}
  {% endfor %}
  {{ button(form.ctaText, 'submit', false, null, null, null ) | safe }}
</form>
